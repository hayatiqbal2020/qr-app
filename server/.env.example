# Server
PORT=5000

# Database (MongoDB Atlas). Use a real username and password; if password has
# special characters (@ # : / etc.), URL-encode them (e.g. @ → %40, # → %23).
# MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@cluster0.xxxxx.mongodb.net/
# DATABASE_URL=postgresql://user:pass@host:5432/payqr

# Auth
# JWT_SECRET=your-secret-key-here
# NODE_ENV=development
